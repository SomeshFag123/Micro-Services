# Example environment variables for the Student Services Dashboard project
# These can be used for configuration during runtime.

# Flask configuration
FLASK_ENV=production
DEBUG=False

# Example database or API settings (not used in this version)
DB_HOST=localhost
DB_PORT=5432
DB_USER=example_user
DB_PASSWORD=example_password

# Example service ports (for clarity)
STUDENT_SERVICE_PORT=5001
COURSE_SERVICE_PORT=5002
FEEDBACK_SERVICE_PORT=5003
